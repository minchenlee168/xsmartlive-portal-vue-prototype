/** Vite define 注入：最後一次 git commit 時間（ISO 字串），TopBar 顯示 prototype 更新時間用 */
declare const __LAST_COMMIT_TIME__: string
/** Vite define 注入：最近 10 筆 commit，格式 `ISO|subject`，TopBar info icon 開 Dialog 顯示 */
declare const __RECENT_COMMITS__: string[]
